{
  "name": "Estuche Virtual 3.0",
  "forwardPorts": [4000, 5173, 5432],
  "portsAttributes": {
    "4000": {
      "visibility": "public",
      "label": "Backend API"
    },
    "5173": {
      "visibility": "public",
      "label": "Frontend (Vite)"
    },
    "5432": {
      "label": "PostgreSQL"
    }
  },
  "postCreateCommand": "npm install -g pnpm && cd be && pnpm install && cp -n .env-example .env || true && cd ../fe && pnpm install && cp -n .env-example .env || true"
}